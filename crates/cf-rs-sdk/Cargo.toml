[package]
name = "cf-rs-sdk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Write Rust functions that run locally on cf-rs and unmodified on Google Cloud Run functions"
repository.workspace = true

[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"

# See cf-rs-core/Cargo.toml's matching comment: cargo machete can't see
# `use cloudevents::...` as a use of the `cloudevents-sdk` package.
[package.metadata.cargo-machete]
ignored = ["cloudevents-sdk"]

[dependencies]
tokio = { workspace = true }
axum = { workspace = true }
http = { workspace = true }
http-body-util = { workspace = true }
cloudevents-sdk = { workspace = true, features = ["axum"] }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
tower = { workspace = true, features = ["util"] }
